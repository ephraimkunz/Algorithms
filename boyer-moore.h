// Boyer-Moore voting algorithm
// Purpose: Find the majority value in an array, if one exists
// Time: O(n), Space: O(1)

#ifndef BOYER_MOORE_H
#define BOYER_MOORE_H

// Returns index of majority item, else -1 if no majority item.
int bm_majority(int array[], int size);

#endif